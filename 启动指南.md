# 知途 — 完整启动指南

## 环境要求
- **Node.js** ≥ 18（去 https://nodejs.org 下载安装，一路下一步）

## 完整步骤（复制粘贴即可）

### 第一步：安装依赖（仅第一次）

假设你解压到了 `D:\知途`，打开 cmd 执行：

```
cd /d D:\知途
npm install
```

等待 1-2 分钟，看到 `added xxx packages` 即可。

---

### 第二步：配置 API Key（仅第一次）

```
copy .env.example .env
notepad .env
```

把这一行：
```
DEEPSEEK_API_KEY=your-deepseek-api-key-here
```
改成你的真实 key，例如：
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxx
```
保存，关闭记事本。

---

### 第三步：启动后端

打开 cmd，执行：

```
cd /d D:\知途
npm start
```

看到 `API server running on http://localhost:1234` 说明启动成功。

**⚠ 这个窗口不要关！**

---

### 第四步：启动前端

再新开一个 cmd，执行：

```
cd /d D:\知途
npx vite --config vite.config.js
```

看到 `http://localhost:4321/` 说明启动成功。

---

### 第五步：打开浏览器

访问 `http://localhost:4321`

**登录账号：**
- 用户名：`admin`
- 密码：`admin123`

---

## 以后怎么用

每次要用的时候，只需要 第三步 + 第四步：

```
cmd 窗口 1：cd /d D:\知途 → npm start
cmd 窗口 2：cd /d D:\知途 → npx vite --config vite.config.js
```

浏览器打开 `localhost:4321`。

---

## 常见问题

**Q: 提示 `'node' 不是内部或外部命令`**
→ 没装 Node.js，去 https://nodejs.org 下载安装

**Q: 提示 `npm install` 报错**
→ 检查网络，或尝试 `npm install --registry=https://registry.npmmirror.com`

**Q: 端口被占用**
→ 后端端口 1234，前端端口 4321，确保没其他程序占用

**Q: AI 对话用不了**
→ 检查 `.env` 里的 `DEEPSEEK_API_KEY` 是否正确
